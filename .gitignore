*.tar.gz
*.zip
*.dmg

*.exe

# SECURITY (audit C5): NEVER commit compiled binaries / release archives to git.
# Distributed binaries belong on the GitHub Release, verified via a GPG-signed
# SHA256SUMS manifest — not in the source tree. The previous re-include
# allow-list (!releases/**/*.{exe,zip,tar.gz}) tracked ~800MB of binaries and
# is intentionally removed. See RELEASE_SECURITY_PROCESS.md.
releases/**/*.exe
releases/**/*.zip
releases/**/*.tar.gz
releases/**/*.dmg
releases/**/*.dylib
# Bare unpacked executables and the macOS resource-fork sidecars that have
# leaked into releases/ (radiantd, radiant-cli, radiant-tx, radiant-wallet,
# radiant-qt, ._* AppleDouble files).
releases/**/radiantd
releases/**/radiant-cli
releases/**/radiant-tx
releases/**/radiant-wallet
releases/**/radiant-qt
releases/**/._*

# SECURITY (audit C5/N1/C4): GUI-executed node binaries must NOT be vendored.
# The GUI downloads + verifies them at runtime against the signed SHA256SUMS
# manifest (fail-closed on placeholder/missing hashes). Keep only source under
# gui/; never the compiled radiantd/radiant-cli/radiant-tx or their dylibs.
gui/binaries/**/*.dylib
gui/binaries/**/radiantd
gui/binaries/**/radiant-cli
gui/binaries/**/radiant-tx
gui/binaries/**/radiant-wallet
gui/binaries/**/radiant-qt
gui/binaries/**/._*
src/radiant
src/radiantd
src/radiant-cli
src/radiant-seeder
src/radiant-tx
src/radiant-wallet
src/test/test_bitcoin
src/test/test_bitcoin_fuzzy
src/qt/test/test_bitcoin-qt

# autoreconf
aclocal.m4
autom4te.cache/
build-aux/config.guess
build-aux/config.sub
build-aux/depcomp
build-aux/install-sh
build-aux/ltmain.sh
build-aux/m4/libtool.m4
build-aux/m4/lt~obsolete.m4
build-aux/m4/ltoptions.m4
build-aux/m4/ltsugar.m4
build-aux/m4/ltversion.m4
build-aux/missing
build-aux/compile
build-aux/test-driver
config.log
config.status
configure
libtool
src/config/bitcoin-config.h
src/config/bitcoin-config.h.in
src/config/stamp-h1
share/setup.nsi
share/qt/Info.plist

src/univalue/gen
src/obj

src/qt/*.moc
src/qt/moc_*.cpp
src/qt/forms/ui_*.h

src/qt/test/moc*.cpp

src/qt/bitcoin-qt.config
src/qt/bitcoin-qt.creator
src/qt/bitcoin-qt.creator.user
src/qt/bitcoin-qt.files
src/qt/bitcoin-qt.includes

.deps
.dirstamp
.libs
.*.swp
*.*~*
*.bak
*.rej
*.orig
*.pyc
*.o
*.o-*
*.a
*.pb.cc
*.pb.h

*.log
*.trs
*.dmg

*.json.h
*.raw.h

# Only ignore unexpected patches
*.patch
!depends/patches/**/*.patch

#libtool object files
*.lo
*.la

# Compilation and Qt preprocessor part
*.qm
Makefile
bitcoin-qt
Bitcoin-Qt.app
background.tiff*

# Unit-tests
Makefile.test
bitcoin-qt_test

# Resources cpp
qrc_*.cpp

# Mac specific
.DS_Store
build
build-*/

#lcov
*.gcno
*.gcda
/*.info
test_bitcoin.coverage/
total.coverage/
coverage_percent.txt

#build tests
linux-coverage-build
linux-build
win32-build
test/config.ini
test/cache/*

!src/leveldb*/Makefile
src/leveldb/CONTRIBUTING.md
src/leveldb/NEWS
src/leveldb/TODO

/doc/doxygen/

/doc/mkdocs.yml
/doc/gen-doc-md.sh
/doc/md/
/doc/html/

libbitcoinconsensus.pc
contrib/devtools/split-debug.sh

# vagrant
.vagrant


#Local Development Environment - NetBeans
nbproject/

#Local Development Environment - Eclipse
.cproject
.project
.pydevproject

#Local Development Environment - Visual Studio Code
.vscode
*.code-workspace

#Local Development Environment - MinGW64
mingw64/

# Build directories
build/
build-ci/
build-local/

# Test cache
ft-cache/
ft-tmp/
test-tmp/
Testing/

# Release artifacts (top-level build dirs only — don't shadow doc/release-notes/)
/release-*/

# Local CMake/Ninja build trees (not for source tree). Distributed binaries go in releases/
# attached to the GitHub Release, not into git.
/macos-arm64-release/

# Blockchain data
data-*/
regtest-data/
testnet-data/
wallets-testnet/

# Runtime files
*.pid

# Dependencies (should be fetched by build system, not tracked)
boost-*/
libevent-2.*/
libevent-build/
libevent-install/

# Local stubs and workarounds
libevent-stubs.c

# Local build scripts
create_build_h.py

# Output files
stdout.txt
stderr.txt

# Local testing scripts (move to radiant-local-testing/)
monitor.sh
run_testnet_miner.sh
start_orbstack.sh

# VS Code workspace
radiant-core.code-workspace

# Windows build artifacts
*.spec
temp_test/
test.cpp
package_*.py
test_package_integration.py
out.txt
msys2-base.tar.xz
build_win/

# Windows local scripts
build_real.bat
gui/build_exe.bat
install_deps_final.bat
repair_msys2.bat
run_msys2_setup.bat
setup_*.bat
setup_msys2.sh

# macOS/GUI build artifacts
macos-app-build/
release-builds/
gui/__pycache__/
gui/*.iconset/
*.app/
gui/build/

# Release output directory
release/

# Build directories (CMake/Ninja)
build-release/
build-debug/
macos-release/
macos-release-arm64/
native/

# Bundled libraries (created by fix-macos-dylibs.sh)
gui/binaries/*/libs/

# py2app artifacts
*.egg-info/

# Local blockchain data and configs
radiant-mainnet-data/
radiant-mainnet.conf
radiant.conf

# Local test scripts and temporary files
mining/test_asic.py
mining/test_full_mining.py

# Build artifacts and releases
radiant-core-windows-x64/

# Network monitoring script (local use only)
network_monitor.py

# Local audit / release-prep working notes
AUDIT_*.md
*.LOCAL.md

# Chain-scan results from contrib/audit/scan-hash-opcode-usage.py (local only;
# the scanner tool itself stays tracked, just not its per-run output)
contrib/audit/scan-hash-opcode-results-*.md

# WebUI frontend build artifacts
src/webui/ui/node_modules/
src/webui/ui/dist/

